erDiagram
    DONORS ||--o{ APPOINTMENTS : makes
    DONORS ||--o{ DONATION_HISTORY : "completes"
    DONORS ||--o{ DONATION_REQUESTS : "receives"
    STAFF ||--o{ DONATION_REQUESTS : "sends"
    STAFF ||--o{ DONATION_HISTORY : "processes"
    APPOINTMENTS ||--o| DONATION_HISTORY : "results in"
    BLOOD_INVENTORY ||--o{ DONATION_HISTORY : "updated by"

    DONORS {
        string id PK
        string email UK
        string password
        string name
        boolean isActive
        date registrationDate
        string bloodType
        date lastDonationDate
        int donationCount
    }

    STAFF {
        string id PK
        string role "staff|admin"
        string email UK
        string password
        string name
        boolean isActive
        date registrationDate
    }

    APPOINTMENTS {
        string id PK
        string donorId FK
        string donorName "denormalized"
        string bloodType "denormalized"
        date date
        string status "pending|confirmed|checked-in|completed|cancelled"
        string confirmationNumber
        date createdDate
        string donationHistoryId FK "optional, when completed"
    }

    BLOOD_INVENTORY {
        string type PK "A+, A-, B+, B-, AB+, AB-, O+, O-"
        int units "total valid units"
        date lastUpdated
        date expirationDate "last expiring batch"
        object batches "array of batch objects"
    }

    DONATION_HISTORY {
        string id PK
        string appointmentId FK "source appointment"
        string donorId FK
        string donorName "denormalized"
        string bloodType "denormalized"
        date date
        int units "amount donated"
        string staffId FK "staff who processed"
        string staffName "denormalized"
        string status "completed"
    }

    DONATION_REQUESTS {
        string id PK
        string bloodType
        string requestedBy FK "staff id"
        string requestedByName "denormalized"
        string donorId FK
        string donorName "denormalized"
        date requestDate
        string status "pending|accepted|declined"
        string message
        date responseDate "optional"
    }
